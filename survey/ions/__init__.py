# survey.ions — per-ion physical facts (code-independent)
# Each module defines KNOWN_PEAKS (List[KnownPeak]) and BASE_CONFIG (Dict[str,int]).
# FAC-specific configuration (CONFIGURATIONS, OPTIMIZE_RADIAL, etc.) lives
# in inputs/target_case_<ion>_survey.py alongside the scoring parameters.
